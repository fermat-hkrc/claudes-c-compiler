# PBT Campaign: encode_rev

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:240 Bit manipulation lists `rev`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:910 `"rev" => encode_rev(operands)` (no NEON/RegArrangement split — vector reverse is rev16/rev32/rev64). Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `rev w0, w1` = 0x5ac00820). Callers: encoder/mod.rs:910 only. The producing statements at bitfield.rs:186-193 are not independent Doc evidence. ARM ARM Data-processing (1 source) REV: REV <Wd>, <Wn> / REV <Xd>, <Xn>; encoding sf 1 0 11010110 00000 opc Rn Rd with opc=000010 (32-bit) / 000011 (64-bit); register 31 is ZR not SP. llvm-mc/gas reject mixed W/X, SP/WSP, FP/SIMD, extra operands, too few operands, and vector `rev Vd.T, Vn.T` (use rev16/rev32/rev64). Unlike REV16/CLZ/RBIT, opcode bits[15:10] change with sf.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_rev16_kat_llvm_mc_x0_x1` (unchanged project harness) → `1 passed; 0 failed; 2247 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_rev_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_rev16_pbt / encode_rev32_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_rev (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_rev; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_bfi / encode_bfxil / encode_extr / encode_clz / encode_cls / encode_rbit / encode_rev16 / encode_rev32
- **State machine:** not applicable — encode_rev is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_rev):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree REV decoder). Sibling encode_rev16 / encode_rev32 / encode_rbit / encode_clz / encode_cls rejected (different opcode 000001 / 000010-X / 000000 / 000100 / 000101; REV uniquely uses 000010-W / 000011-X). encode_neon_two_misc rejected (vector rev16/rev32/rev64, different ARM class; public dispatch does not route `rev` there). Weaker available: algebraic.metamorphic (Rd/Rn field independence; W vs X flips sf AND opcode LSB), algebraic.invariant (ARM fields), negative_error (arity / extra / SP / mixed / FP). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for `rev`; mapping `[Reg(Rd), Reg(Rn)]` <-> `rev Rd, Rn`.
- **Seeds:** bitfield.rs encode_rev16_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_rev unit tests. README.md:240 lists rev.

## Module: encode_rev
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_rev (invalid-name / nonreg / alt-spellings). Added encode_rev_diff_alt_spellings, encode_rev_neg_nonreg, encode_rev_neg_invalid_name (passing). Closed: tier round spent and documented surface covered.
