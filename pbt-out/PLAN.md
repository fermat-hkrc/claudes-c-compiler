# PBT Campaign: encode_extr

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:216 Bit fields lists `extr`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:894 `"extr" => encode_extr(operands)`. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `extr w0, w1, w2, #0` = 0x13820020). Callers: encoder/mod.rs:894 only. The producing statements at bitfield.rs:133-144 are not independent Doc evidence. ARM ARM Extract EXTR: EXTR <Wd>, <Wn>, <Wm>, #<lsb> / EXTR <Xd>, <Xn>, <Xm>, #<lsb>; encoding sf 00 100111 N 0 Rm imms Rn Rd with N=sf; 0 <= lsb <= 31 (W) / 63 (X); register 31 is ZR not SP. ROR (immediate) is the ARM alias of EXTR when Rn=Rm. llvm-mc/gas reject extra operands, mixed W/X, SP/WSP, FP/SIMD, too few operands, and out-of-range lsb.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_clz_kat_llvm_mc_w0_w1` (unchanged project harness) → `1 passed; 0 failed; 2101 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_extr_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_clz_pbt / encode_cls_pbt / encode_bfi_pbt / encode_bfxil_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_clz_pbt / encode_cls_pbt / encode_bfi_pbt / encode_bfxil_pbt modules.
- **Candidate modules:** encode_extr (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_extr; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_bfi / encode_bfxil / encode_cls / encode_clz
- **State machine:** not applicable — encode_extr is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_extr):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree EXTR decoder). Sibling encode_shift ROR rejected as differential (same-job gate: ROR is a 3-operand shift mnemonic; EXTR is 4-operand extract). encode_ubfx/sbfx/bfm rejected (different opc / instruction class). Weaker available: algebraic.metamorphic (ARM ROR alias when Rn=Rm; Rd/Rn/Rm field independence), algebraic.invariant (ARM fields), negative_error (arity / extra / SP / mixed width / FP / lsb range / nonreg / invalid-name). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for `extr`; mapping `[Reg(Rd), Reg(Rn), Reg(Rm), Imm(lsb)]` <-> `extr Rd, Rn, Rm, #lsb`.
- **Seeds:** bitfield.rs encode_bfi_pbt / encode_clz_pbt (llvm-mc differential, ARM fields, extra/SP/mixed/FP/lsb negative contracts). No dedicated encode_extr unit tests. README.md:216 lists `extr`.

## Module: encode_extr
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_extr (arity / extra / SP / mixed W-X / FP / lsb / nonreg / invalid-name / alt-spellings). Added encode_extr_diff_alt_spellings, encode_extr_neg_mixed_width, encode_extr_neg_fp, encode_extr_neg_nonreg, encode_extr_neg_invalid_name (3 passing, 2 failing). Closed: tier round spent and documented surface covered.
