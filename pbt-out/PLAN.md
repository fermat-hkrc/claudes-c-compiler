# PBT Campaign: encode_fmov

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:223 Floating point lists `fmov`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:376 `"fmov" => encode_fmov(operands)`. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `fmov s0, s1` = 0x1e204020 and `fmov s0, w1` = 0x1e270020). Callers: encoder/mod.rs:376 only. Codegen emits `fmov sN, wN` / `fmov dN, xN` / reverse / `fmov sN, sN` style register forms (float_ops.rs, cast_ops.rs, alu.rs, asm_emitter.rs:205 "fmov requires d/s register form, not v"). The producing statements at fp_scalar.rs:6-65 are not independent Doc evidence. ARM ARM FMOV (register): 0 00 11110 ftype 1 000000 10000 Rn Rd (ftype 00=S, 01=D, 11=H). ARM ARM FMOV (general): sf 00 11110 ftype 1 rmode opcode 000000 Rn Rd with opcode 111 GP->FP / 110 FP->GP; matching widths Sd/Wn, Dd/Xn, Hd/Wn. Register 31 is ZR not SP. llvm-mc/gas reject extra operands, mixed S/D, size-mismatched GP/FP (D/W, S/X), Q/B/V scalar, SP/WSP, and too few operands. Immediate FMOV is accepted by gas/llvm-mc; SUT has an explicit TODO "not yet supported" (fp_scalar.rs:14-15).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_fcvt_rounding_kat_llvm_mc_fcvtzs_w0_s1` (unchanged project harness) → `1 passed; 0 failed; 2127 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_fmov_pbt` at the bottom of `src/backend/arm/assembler/encoder/fp_scalar.rs` (same convention as neighbouring encode_fcvt_rounding_pbt / encode_fp_1src_pbt / encode_int_to_float_pbt / encode_fcmp_pbt / encode_fcvt_precision_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_fmov (single-symbol campaign on fp_scalar.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_fmov; other fp_scalar.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_fcvt_rounding / encode_fp_1src / encode_int_to_float / encode_fcmp / encode_fcvt_precision
- **State machine:** not applicable — encode_fmov is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_fmov):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree FMOV decoder). Sibling encode_fp_arith / encode_fneg / encode_fp_1src rejected as differential (same-job gate: different mnemonics / ARM classes). Weaker available: algebraic.metamorphic (Rd/Rn field independence; S vs D ftype; GP->FP vs FP->GP opcode), algebraic.invariant (ARM fields), negative_error (arity / extra / mixed S-D / size mismatch / SP / QVB / nonreg / invalid-name). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for `fmov`; mapping `[Reg(Rd), Reg(Rn)]` <-> `fmov Rd, Rn`.
- **Seeds:** fp_scalar.rs encode_fcvt_rounding_pbt / encode_fp_1src_pbt (llvm-mc differential, ARM fields, extra/SP/wrong-type negative contracts). No dedicated encode_fmov unit tests. README.md:223 lists `fmov`. Codegen float_ops.rs:27-36 emits the six scalar register forms.

## Module: encode_fmov
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_fmov (nonreg / invalid-name / V.D[1] / immediate TODO). Added encode_fmov_neg_nonreg (passing), encode_fmov_neg_invalid_name (passing), encode_fmov_diff_vd1 (failing). Immediate form recorded as Design Caveat (fp_scalar.rs:14-15). Closed: tier round spent and documented surface covered.
