# PBT Campaign: encode_neon_float_three_same

## Scan findings
- **Spec:** (none found as a standalone requirement/dev-task document). In-tree contract for this symbol:
  - Encoder module docstring at `src/backend/arm/assembler/encoder/mod.rs:1-7`: Encodes AArch64 instructions into 32-bit machine code words; covers the subset emitted by codegen; always 4 bytes little-endian.
  - Assembler README `src/backend/arm/assembler/README.md:5-14`: accepts the same textual assembly that GCC's gas would consume; GNU-style assembly text as emitted by AArch64 codegen.
  - README.md:226 NEON float vector table lists `fadd`, `fsub`, `fmul`, `fdiv`, `fmax`, `fmin`, `fmaxnm`, `fminnm`, `frecps`, `frsqrts` (vector forms).
  - Dispatch `encoder/mod.rs:377-496`: vector `RegArrangement` dest routes fadd/fsub/fmul/fdiv/fmax/fmin/fmaxnm/fminnm/fmla/fmls/frecps/frsqrts/fcmeq/fcmge/fcmgt (register form)/facge/facgt to `encode_neon_float_three_same` with ARM-correct (U, size_hi, opcode).
  - Body at `neon.rs:1388-1403`: Encode NEON float three-same. Format claimed: `0 Q U 01110 size 1 Rm opcode 1 Rn Rd` with size[1]=size_hi, size[0]=sz (0=single, 1=double). T from dest arrangement: 2s→(Q=0,sz=0), 4s→(Q=1,sz=0), 2d→(Q=1,sz=1); other T Err. No arity check (unlike integer `encode_neon_three_same`). Source arrangements discarded.
  - ARM ARM Advanced SIMD three-same (FP): `0 Q U 01110 size 1 Rm opcode 1 Rn Rd`. Valid T: 2S, 4S, 2D. 1D reserved. FP16 4H/8H is a different encoding (size=0x with FEAT_FP16). Scalar `fadd s0, s1, s2` / `fadd d0, d1, d2` is scalar FP (different class). Exactly three operands. V registers only.
  - llvm-mc `-triple=aarch64` confirms: `fadd v0.4s, v1.4s, v2.4s` = 0x4e22d420; `fadd v0.2s, v1.2s, v2.2s` = 0x0e22d420; `fadd v0.2d, v1.2d, v2.2d` = 0x4e62d420; `fsub v0.4s, v1.4s, v2.4s` = 0x4ea2d420; `fmul v0.4s, v1.4s, v2.4s` = 0x6e22dc20. Rejects extra operand, 8b/16b/4h/8h/1d, mismatched T, v32, non-V dest, GPR as Vm.
  - `get_neon_reg` (`neon.rs:7-21`) accepts Operand::RegArrangement or Operand::Reg; `parse_reg_num` (`encoder/mod.rs:131-148`) maps x/w/d/s/q/v/h/b prefixes with num<=31. Sibling `encode_neon_three_same` is integer three-same (different size map). Sibling `encode_neon_float_cmp_zero` is two-reg misc compare-to-zero (different job). Sibling `encode_fp_arith` is scalar FP (different job).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod <name>` inside source files, discovered by `cargo test --lib`. Filename convention: `#[test] fn` inside the source file's test module. `proptest` is already a `[dev-dependencies]` entry (`Cargo.toml`). Historical `pbt-out/`/`pbt-native/` excluded from layout discovery. No crate-root `tests/` directory. `neon.rs` already has prior-campaign modules `encode_neon_three_diff_narrow_pbt`, `encode_neon_across_long_pbt`, `encode_neon_float_cmp_zero_pbt`, `encode_neon_sli_pbt` — not rewritten.
- **Buildability probe:** `cargo test --lib encode_neon_float_cmp_zero_kat -- --test-threads=${PBT_TEST_JOBS}` in `/home/toan/github/claudes-c-compiler` → `test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 1102 filtered out`. Rung 1 available; project cargo harness builds and runs.
- **Harness placement:** extend existing `cargo test --lib` target (rung 1) by appending a new `#[cfg(test)] mod encode_neon_float_three_same_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (inline layout; sibling of `encode_neon_float_cmp_zero_pbt` / `encode_neon_sli_pbt`). Reuse the existing `proptest` dev-dependency. Not pbt-native: project cargo harness builds and runs. Historical `pbt-out/`/`pbt-native/` were excluded from layout discovery. Rung 1 was available (probe passed).
- **Candidate modules:** encode_neon_float_three_same (neon.rs)
- **Skipped modules:** (none) — campaign scoped to this single symbol; other functions in neon.rs are indexed but not tested.
- **Oracle (encode_neon_float_three_same):** Differential (llvm-mc -triple=aarch64 -show-encoding) for valid vector FP three-same. State machine rejected: pure function, no lifecycle. Round-trip with an in-tree FP three-same decoder rejected: none exists. encode_neon_three_same / encode_neon_float_cmp_zero / encode_fp_arith fail the same-job sibling gate (integer three-same vs FP three-same; compare-to-zero two-misc vs three-same; scalar FP vs vector). SUT-boundary: internal-helper of the GNU-style assembler; public contract is gas-compatible AArch64 text. Mapping: `[RegArrangement(Vd,T), RegArrangement(Vn,T), RegArrangement(Vm,T)] + (U, size_hi, opcode)` <-> `{fadd|fsub|fmul|...} Vd.T, Vn.T, Vm.T`.
- **Seeds:** encode_neon_float_cmp_zero_pbt (same file): differential vs llvm-mc, ARM field unpack, U/size_hi metamorphic, extra-operand / invalid-T / invalid-name / non-V / mismatch negatives. No existing unit test of encode_neon_float_three_same. Seed: encode_neon_float_cmp_zero_pbt::encode_neon_float_cmp_zero (diff / extra / mismatch).
- **State machine:** not applicable — encode_neon_float_three_same is a pure function with no mutating operations or lifecycle.

## Module: encode_neon_float_three_same
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results (1 coverage-sweep round: coverage_gaps had no profraw; manual arm audit of get_neon_reg dest/Vn/Vm, match 2s/4s/2d/_, arity — all reached)
