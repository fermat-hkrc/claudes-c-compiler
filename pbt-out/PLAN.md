# PBT Campaign: encode_ldxp_stxp

## Scan findings
- **Spec:** (none found as a standalone requirement/dev-task document). In-tree contract for this symbol:
  - Encoder module docstring at `src/backend/arm/assembler/encoder/mod.rs:1-7`: Encodes AArch64 instructions into 32-bit machine code words; covers the subset emitted by codegen; always 4 bytes little-endian.
  - Assembler README `src/backend/arm/assembler/README.md:5-14`: accepts the same textual assembly that GCC's gas would consume; GNU-style assembly text as emitted by AArch64 codegen.
  - Loads/Stores table at README.md:221 lists ldxr/stxr/ldaxr/stlxr/ldar/stlr family but not ldxp/stxp/ldaxp/stlxp; dispatch still owns them.
  - Dispatch `encoder/mod.rs:366-369`: `"ldxp" => encode_ldxp_stxp(operands, true, false)`, `"ldaxp" => encode_ldxp_stxp(operands, true, true)`, `"stxp" => encode_ldxp_stxp(operands, false, false)`, `"stlxp" => encode_ldxp_stxp(operands, false, true)`.
  - Body at `load_store.rs:604-634`: Encode LDXP/STXP/LDAXP/STLXP (exclusive pair). Load arity Rt, Rt2, [Rn]; store arity Ws, Rt, Rt2, [Rn]. sz from Rt width (X→1, W→0); o0 from acquire_release; Rs=11111 on load. Format claimed: `1 sz 001000 0 L 1 Rs o0 Rt2 Rn Rt` with L=1 load / L=0 store.
  - ARM ARM Load/Store Exclusive Pair: `size 001000 0 L 1 Rs o0 Rt2 Rn Rt` with size=10 (W pair) or 11 (X pair). Rt/Rt2 are Wt/Xt (31=ZR, never SP). Rn is Xn|SP (never XZR, never W). Ws is Wt (31=WZR, never X, never SP). Offset absent or #0. llvm-mc `-triple=aarch64` confirms: `ldxp x0, x1, [x2]` = 0xc87f0440; `ldxp w0, w1, [x2]` = 0x887f0440; `ldaxp x0, x1, [x2]` = 0xc87f8440; `stxp w0, x1, x2, [x3]` = 0xc8200861; `stlxp w0, x1, x2, [x3]` = 0xc8208861; `ldxp x0, x1, [sp]` = 0xc87f07e0; `ldxp xzr, x1, [x2]` = 0xc87f045f; `stxp wzr, x1, x2, [x3]` = 0xc83f0861; `ldxp lr, x1, [x2]` accepted as `ldxp x30, x1, [x2]` (0xc87f045e). Rejects: extra operand, SP as Rt/Rt2/Ws, [xzr] as base, W-register base, wsp base, offset != 0, pre/post-index, mixed X/W data pair, X as STXP status, SIMD Rt, `ldxp v0, ...`. Same-number Rt==Rt2 is accepted by llvm-mc (`ldxp x0, x0, [x2]`).
  - `get_reg` (`encoder/mod.rs:956-964`) requires Operand::Reg; `parse_reg_num` (`encoder/mod.rs:131-148`) maps sp/wsp/xzr/wzr→31, lr→30, and x/w/d/s/q/v/h/b prefixes with num<=31. `is_64bit_reg` (`encoder/mod.rs:150-154`) is prefix x or sp/xzr/lr. Sibling `encode_ldxr_stxr` / `encode_ldaxr_stlxr` are exclusive-single (different job). Sibling `encode_ldp_stp` is non-exclusive pair (different job).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod <name>` inside source files, discovered by `cargo test --lib`. Filename convention: `#[test] fn` inside the source file's test module. `proptest` is already a `[dev-dependencies]` entry (`Cargo.toml`). Historical `pbt-out/`/`pbt-native/` excluded from layout discovery. No crate-root `tests/` directory. `load_store.rs` already has prior-campaign modules `encode_adr_pbt`, `encode_ldar_stlr_pbt`, `encode_ldur_stur_pbt` — not rewritten.
- **Buildability probe:** `cargo test --lib encode_ldar_stlr_kat -- --test-threads=${PBT_TEST_JOBS}` in `/home/toan/github/claudes-c-compiler` → `test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 1074 filtered out`. Rung 1 available; project cargo harness builds and runs.
- **Harness placement:** extend existing `cargo test --lib` target (rung 1) by appending a new `#[cfg(test)] mod encode_ldxp_stxp_pbt` at the bottom of `src/backend/arm/assembler/encoder/load_store.rs` (inline layout; sibling of `encode_ldar_stlr_pbt` / `encode_ldur_stur_pbt`). Reuse the existing `proptest` dev-dependency. Not pbt-native: project cargo harness builds and runs. Historical `pbt-out/`/`pbt-native/` were excluded from layout discovery. Rung 1 was available (probe passed).
- **Candidate modules:** encode_ldxp_stxp (load_store.rs)
- **Skipped modules:** (none) — campaign scoped to this single symbol; other functions in load_store.rs are indexed but not tested.
- **Oracle (encode_ldxp_stxp):** Differential (llvm-mc -triple=aarch64 -show-encoding) for valid LDXP/LDAXP/STXP/STLXP. State machine rejected: pure function, no lifecycle. Round-trip with an in-tree exclusive-pair decoder rejected: none exists. encode_ldxr_stxr / encode_ldaxr_stlxr / encode_ldp_stp fail the same-job sibling gate (exclusive-single vs exclusive-pair; non-exclusive pair vs exclusive pair). SUT-boundary: internal-helper of the GNU-style assembler; public contract is gas-compatible AArch64 text. Mapping: load `[Reg(Rt), Reg(Rt2), Mem{Rn, 0}, is_load=true, acquire_release]` <-> `{ldxp|ldaxp} Rt, Rt2, [Rn]`; store `[Reg(Ws), Reg(Rt), Reg(Rt2), Mem{Rn, 0}, is_load=false, acquire_release]` <-> `{stxp|stlxp} Ws, Rt, Rt2, [Rn]`.
- **Seeds:** encode_ldar_stlr_pbt and encode_ldur_stur_pbt (same file): differential vs llvm-mc, ARM field unpack, extra-operand / invalid-Rt / invalid-base / W-base negatives. No existing unit test of encode_ldxp_stxp. Seed: encode_ldar_stlr_pbt::encode_ldar_stlr (diff / extra / invalid base).
- **State machine:** not applicable — encode_ldxp_stxp is a pure function with no mutating operations or lifecycle.

## Module: encode_ldxp_stxp
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results (1 coverage-sweep round: coverage_gaps had no profraw; manual arm audit of too-few / non-Reg / non-Mem / parse_reg_num None — passing)
