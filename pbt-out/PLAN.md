# PBT Campaign: encode_neon_shift_left_imm

## Scan findings
- **Spec:** (none found as a standalone requirement/dev-task document). In-tree contract for this symbol:
  - Assembler README `src/backend/arm/assembler/README.md:1-14`: built-in AArch64 assembler translates GNU-style assembly text; "accepts the same textual assembly that GCC's gas would consume".
  - README instruction table `src/backend/arm/assembler/README.md:224` NEON three-same lists `sqshl`, `uqshl` (register form); `README.md:228` NEON shifts lists `shl`, `sqshl`, `uqshl`, `sqshlu` (immediate form).
  - Encoder module docstring `src/backend/arm/assembler/encoder/mod.rs:1-7`: encodes AArch64 instructions into 32-bit machine code words.
  - Dispatch `encoder/mod.rs:544-551`: `"sqshl"` with Imm third operand => `encode_neon_shift_left_imm(operands, 0, 0b01110)`; `"uqshl"` with Imm => `encode_neon_shift_left_imm(operands, 1, 0b01110)`. Register-form sqshl/uqshl go to `encode_neon_three_same` (different job).
  - Body at `neon.rs:1740-1774`: comment "NEON shift left by immediate (SQSHL, UQSHL, SHL, etc.)"; format `0 Q U 011110 immh:immb opcode 1 Rn Rd`; `immh:immb = esize + shift_amount`; T from dest arrangement only (`8b/16b/4h/8h/2s/4s/2d`); source arrangement discarded; extra operands beyond index 2 ignored (`len() < 3`); no shift-range check; `shift as u32` truncates i64.
  - ARM ARM Advanced SIMD shift left (immediate): SQSHL U=0 opcode=01110; UQSHL U=1 opcode=01110. Shift range `[0, esize-1]`. Vector T in `{8B,16B,4H,8H,2S,4S,2D}` (no 1D). Scalar SQSHL Bd/Hd/Sd/Dd uses a different encoding (bits[31:29]=010) and is not this helper.
  - llvm-mc `-triple=aarch64` (probed): `sqshl v0.8b, v1.8b, #0` = 0x0f087420; `#7` = 0x0f0f7420; `uqshl v0.8b, v1.8b, #0` = 0x2f087420; `sqshl v0.16b, v1.16b, #3` = 0x4f0b7420; `sqshl v0.4h, v1.4h, #15` = 0x0f1f7420; `sqshl v0.2d, v1.2d, #0` = 0x4f407420; `#63` = 0x4f7f7420; `uqshl v31.4s, v30.4s, #31` = 0x6f3f77df. OOB shift, extra operand, mismatched T, 1d, v32, 8s all error. Scalar `sqshl d0, d1, #0` = 0x5f407420 (different encoding).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod <name>` inside source files, discovered by `cargo test --lib`. Filename convention: `#[test] fn` inside the source file's test module. `proptest` is already a `[dev-dependencies]` entry (`Cargo.toml`). Historical `pbt-out/`/`pbt-native/` excluded from layout discovery. No crate-root `tests/` directory. `neon.rs` already hosts neighbouring PBT modules (`encode_neon_sli_pbt`, `encode_neon_shift_imm_pbt`, …). New work extends the same `cargo test --lib` inline layout.
- **Buildability probe:** `cargo test --lib test_ascii_passthrough -- --test-threads=${PBT_TEST_JOBS}` in `/home/toan/github/claudes-c-compiler` → `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1616 filtered out`. Rung 1 available; project cargo harness builds and runs.
- **Harness placement:** extend existing `cargo test --lib` target (rung 1) by appending a new `#[cfg(test)] mod encode_neon_shift_left_imm_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (inline layout). Reuse the existing `proptest` dev-dependency. Not pbt-native: project cargo harness builds and runs. Historical `pbt-out/`/`pbt-native/` were excluded from layout discovery. Rung 1 was available (probe passed).
- **Candidate modules:** encode_neon_shift_left_imm (neon.rs)
- **Skipped modules:** (none) — campaign scoped to this single symbol; other functions in neon.rs are indexed but not tested.
- **Oracle (encode_neon_shift_left_imm):** Differential (llvm-mc -triple=aarch64 -show-encoding) for valid GNU-style SQSHL/UQSHL Vd.T, Vn.T, #shift. State machine rejected: single encoding call, no lifecycle/state enum. Round-trip with an in-tree decoder rejected: none exists. encode_neon_shl / encode_neon_sli fail the same-job gate (SHL opcode=01010 U=0; SLI opcode=01010 U=1 — different instructions). encode_neon_three_same is the register-shift sibling, not the immediate form. SUT-boundary: internal-helper of the GNU-style AArch64 assembler; public contract is encoding GNU-style `sqshl`/`uqshl` immediate text. Mapping: `encode_neon_shift_left_imm([RegArrangement(Vd,T), RegArrangement(Vn,T), Imm(shift)], u, 0b01110)` <-> `{sqshl|uqshl} Vd.T, Vn.T, #shift`.
- **Seeds:** README.md:224/228 tables; encoder/mod.rs:544-551 dispatch; ARM ARM SQSHL/UQSHL (immediate); llvm-mc KAT encodings; neighbouring encode_neon_sli_pbt (same shift-left-immediate family). No existing unit test of encode_neon_shift_left_imm.
- **State machine:** not applicable — encode_neon_shift_left_imm is a single-call encoder with no mutating operation alphabet whose order can corrupt encoder-owned instruction state beyond appending a word.

## Module: encode_neon_shift_left_imm
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results (1 coverage-sweep round: coverage_gaps had no profraw; manual arm audit of extra / shift range / T mismatch / non-V prefix / Operand::Reg — added encode_neon_shift_left_imm_neg_non_v_prefix, encode_neon_shift_left_imm_neg_reg_no_arrangement)
