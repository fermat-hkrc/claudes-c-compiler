# PBT Campaign: encode_cas

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:242 LSE Atomics lists `cas` with acquire/release/byte/halfword variants, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:920-922 dispatch of cas/casa/casal/casl/casb/casab/casalb/caslb/cash/casah/casalh/caslh to encode_cas. Purpose comment at load_store.rs:811-813: Encode CAS/CASA/CASAL/CASL and byte/halfword variants; CAS Xs, Xt, [Xn]: size 001000 1 L 1 Rs o0 11111 Rn Rt. Independent assembler reference: llvm-mc -triple=aarch64 -mattr=+lse -show-encoding (gas aarch64-linux-gnu-as -march=armv8-a+lse agrees on `cas x0, x1, [x2]` = 0xc8a07c41). Callers: encoder/mod.rs:920-922 only. The producing statements at load_store.rs:818-843 are not independent Doc evidence; the encoding comment at 813 is a purpose comment. ARM ARM Compare and Swap (CAS): CAS Ws,Wt,[Xn|SP{,#0}] / CAS Xs,Xt,[Xn|SP{,#0}]; CASB/CASH require W registers; encoding size 001000 1 L 1 Rs o0 11111 Rn Rt; size 00=byte 01=half 10=word 11=doubleword; L=1 acquire (CASA/CASAL); o0=1 release (CASL/CASAL); Rs/Rt are ZR not SP at 31; Rn is SP not ZR at 31. llvm-mc/gas reject extra operands, mixed W/X, SP/WSP as Rs/Rt, W/WSP/XZR/x31 as base, FP/SIMD, casb/cash with X, nonzero offset, pre/post-index, register-offset, and too few operands. gas accepts optional `#0` offset (ARM `{,#0}`); llvm-mc 15 rejects `#0`.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_bfxil_kat_llvm_mc_w0_w1_lsb0_width1` (unchanged project harness) → `1 passed; 0 failed; 2033 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_cas_pbt` at the bottom of `src/backend/arm/assembler/encoder/load_store.rs` (same convention as neighbouring encode_prfm_pbt / encode_ldar_stlr_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules.
- **Candidate modules:** encode_cas (single-symbol campaign on load_store.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_cas; other load_store.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_adr / encode_ldar_stlr / encode_ldur_stur / encode_ldxp_stxp / encode_ldxr_stxr / encode_ldaxr_stlxr / encode_ldrsw / encode_ldtr_sized / encode_prfm
- **State machine:** not applicable — encode_cas is a pure function (mnemonic + operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_cas):** Differential vs llvm-mc `-triple=aarch64 -mattr=+lse -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree CAS decoder). Sibling encode_swp rejected as differential (same-job gate: SWP is a different LSE opcode class, size 111000 not 001000). Sibling encode_ldar_stlr rejected (LDAR/STLR, bit21=0, no Rs). Weaker available: algebraic.invariant (ARM field unpack), algebraic.metamorphic (Rs/Rt/Rn field independence; L/o0; optional #0 offset), negative_error (arity / extra / SP-as-RsRt / XZR-as-base / W-base / mixed width / FP / casb-X / nonzero offset / non-mem). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for the 12 cas* mnemonics; mapping `[Reg(Rs), Reg(Rt), Mem{Xn|SP, 0}]` <-> `cas* Rs, Rt, [Xn|SP]`.
- **Seeds:** load_store.rs encode_ldar_stlr_pbt / encode_prfm_pbt (llvm-mc differential, ARM fields, extra/SP/W-base negative contracts). No dedicated encode_cas unit tests. README.md:242 lists `cas`.

## Module: encode_cas
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_cas (arity / extra / SP / XZR-base / W-base / mixed W-X / FP / casb-X / nonzero offset / non-mem / invalid-name / alt-spellings). Added encode_cas_neg_invalid_name, encode_cas_diff_alt_spellings (both passing). Closed: tier round spent and documented surface covered.
