# PBT Campaign: encode_neon_float_cmp_zero

## Scan findings
- **Spec:** (none found as a standalone requirement/dev-task document). In-tree contract for this symbol:
  - Encoder module docstring at `src/backend/arm/assembler/encoder/mod.rs:1-7`: Encodes AArch64 instructions into 32-bit machine code words; covers the subset emitted by codegen; always 4 bytes little-endian.
  - Assembler README `src/backend/arm/assembler/README.md:5-14`: accepts the same textual assembly that GCC's gas would consume; GNU-style assembly text as emitted by AArch64 codegen.
  - Assembler README `src/backend/arm/assembler/README.md:227`: NEON compare-zero category lists `fcmeq`/`fcmgt`/`fcmle`/`fcmlt` `#0.0` (vector compare-to-zero). `fcmge #0.0` is dispatched the same way at `encoder/mod.rs:483-484`.
  - Dispatch `encoder/mod.rs:478-494`: `"fcmeq"`/`"fcmge"`/`"fcmgt"` with `operands[2] == Imm(0)` and `"fcmle"`/`"fcmlt"` always → `encode_neon_float_cmp_zero(operands, U, size_hi, opcode)`.
  - Body at `neon.rs:1573-1588`: `FCMEQ/FCMLE/FCMLT/FCMGE/FCMGT to zero`. Format `0 Q U 01110 size 10000 opcode 10 Rn Rd (float, size = 0sz)`. No arity check. Dest Q/sz from arrangement `2s`/`4s`/`2d` only; src arrangement ignored. `size = (size_hi << 1) | sz`.
  - ARM ARM Advanced SIMD two-register miscellaneous, floating-point compare with zero (`FCMEQ/FCMGE/FCMGT/FCMLE/FCMLT <Vd>.<T>, <Vn>.<T>, #0.0`). Encoding: `0 Q U 01110 size 10000 opcode 10 Rn Rd` with **size = 1sz** (sz=0 single, sz=1 double). Valid T: 2S (Q=0,sz=0), 4S (Q=1,sz=0), 2D (Q=1,sz=1). ARM-correct (U, size_hi, opcode): FCMEQ (0,1,01101), FCMGE (1,1,01100), FCMGT (0,1,01100), FCMLE (1,1,01101), FCMLT (0,1,01110). llvm-mc `-triple=aarch64` confirms: `fcmeq v0.4s, v1.4s, #0.0` = 0x4ea0d820; `fcmge v0.4s, v1.4s, #0.0` = 0x6ea0c820; `fcmgt v0.4s, v1.4s, #0.0` = 0x4ea0c820; `fcmle v0.4s, v1.4s, #0.0` = 0x6ea0d820; `fcmlt v0.4s, v1.4s, #0.0` = 0x4ea0e820. Rejects: extra operand, T mismatch, T in {8b,16b,4h,8h,1d,1s,3s}, v32, GPR dest, missing #0.0. Scalar `fcmeq s0, s1, #0.0` is a different encoding (bits[31:30]=01) — not this vector helper's format.
  - `get_neon_reg` (`neon.rs:7-21`) accepts `RegArrangement` or `Reg` (empty arrangement); `parse_reg_num` (`encoder/mod.rs:131-148`) maps sp/wsp/xzr/wzr→31, lr→30, and x/w/d/s/q/v/h/b prefixes with num<=31.
  - No codegen emission of these mnemonics (assembler surface only). Integer sibling `encode_neon_cmp_zero` (`neon.rs:189`) has an arity `< 2` check and uses `neon_arr_to_q_size` (8b..2d); different job (integer compare-to-zero).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod <name>` inside source files, discovered by `cargo test --lib`. Filename convention: `#[test] fn` inside the source file's test module. `proptest` is already a `[dev-dependencies]` entry (`Cargo.toml`). Historical `pbt-out/`/`pbt-native/` excluded from layout discovery. No crate-root `tests/` directory. `neon.rs` already has prior-campaign modules `encode_neon_three_diff_narrow_pbt` and `encode_neon_across_long_pbt` — not rewritten.
- **Buildability probe:** `cargo test --lib encode_neon_across_long_kat_llvm_mc_saddlv_h0_v1_8b -- --test-threads=${PBT_TEST_JOBS}` in `/home/toan/github/claudes-c-compiler` → `test result: ok. 1 passed; 0 failed; 1026 filtered out`. Rung 1 available; project cargo harness builds and runs.
- **Harness placement:** extend existing `cargo test --lib` target (rung 1) by appending a new `#[cfg(test)] mod encode_neon_float_cmp_zero_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (inline layout; sibling of `encode_neon_across_long_pbt`). Reuse the existing `proptest` dev-dependency. Not pbt-native: project cargo harness builds and runs. Historical `pbt-out/`/`pbt-native/` were excluded from layout discovery. Rung 1 was available (probe passed).
- **Candidate modules:** encode_neon_float_cmp_zero (neon.rs)
- **Skipped modules:** (none) — campaign scoped to this single symbol; other functions in neon.rs are indexed but not tested.
- **Oracle (encode_neon_float_cmp_zero):** Differential (llvm-mc -triple=aarch64 -show-encoding) for valid FCMEQ/FCMGE/FCMGT/FCMLE/FCMLT with T in {2S,4S,2D}, Vd/Vn in v0–v31, and ARM-correct (U, size_hi=1, opcode). State machine rejected: pure function, no lifecycle. Round-trip with an in-tree FCMEQ-zero decoder rejected: none exists. encode_neon_cmp_zero / encode_neon_float_two_misc fail the same-job sibling gate (integer compare-zero vs FP two-misc; different size map and opcodes). SUT-boundary: internal-helper of the GNU-style assembler; public contract is gas-compatible AArch64 text. Mapping: `[RegArrangement(Vd, T), RegArrangement(Vn, T)]` + (U, size_hi=1, opcode) <-> `{fcmeq|fcmge|fcmgt|fcmle|fcmlt} Vd.T, Vn.T, #0.0`.
- **Seeds:** encode_neon_across_long_pbt (same file): differential vs llvm-mc, ARM field unpack, extra-operand / invalid-arrangement / dest-type negatives. README.md:227 lists fcmeq/fcmgt/fcmle/fcmlt #0.0. No existing unit test of encode_neon_float_cmp_zero. Seed: encode_neon_across_long_pbt::encode_neon_across_long_diff_llvm_mc at neon.rs encode_neon_across_long_pbt.
- **State machine:** not applicable — encode_neon_float_cmp_zero is a pure function with no mutating operations or lifecycle.

## Module: encode_neon_float_cmp_zero
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results (1 coverage-sweep round: coverage_gaps had no profraw; manual arm audit of get_neon_reg Operand::Reg dest and non-V prefix — dest Reg Err as specified; non-V prefix is bug 3)
