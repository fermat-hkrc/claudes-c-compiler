# PBT Campaign: encode_rev32

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:240 Bit manipulation lists `rev32`, README.md:225 NEON two-misc also lists `rev32`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:579-581 `"rev32"` + first operand RegArrangement => encode_neon_two_misc else encode_rev32. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `rev32 x0, x1` = 0xdac00820). Callers: encoder/mod.rs:579-581 only (scalar). The producing statements at bitfield.rs:205-221 are not independent Doc evidence. ARM ARM Data-processing (1 source) REV32: REV32 <Xd>, <Xn> only (no 32-bit W form); encoding 1 1 0 11010110 00000 000010 Rn Rd; register 31 is ZR not SP. ARM ARM Advanced SIMD two-register miscellaneous REV32: REV32 <Vd>.<T>, <Vn>.<T> with T in {8B,16B,4H,8H}. llvm-mc/gas reject W registers, extra operands, mixed W/X, SP/WSP, FP/SIMD, too few operands, and NEON T in {2S,4S,2D,1D}. Vector REV32 is a different ARM class; public dispatch routes RegArrangement to encode_neon_two_misc, but encode_rev32 itself still contains the NEON path (purpose comment bitfield.rs:206).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_rev16_kat_llvm_mc_x0_x1` (unchanged project harness) → `1 passed; 0 failed; 2219 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_rev32_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_rev16_pbt / encode_rbit_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_rev32 (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_rev32; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_bfi / encode_bfxil / encode_extr / encode_clz / encode_cls / encode_rbit / encode_rev16
- **State machine:** not applicable — encode_rev32 is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_rev32):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree REV32 decoder). Sibling encode_neon_two_misc rejected as differential for the scalar path (same-job gate: vector/RegArrangement, different ARM class; dispatch already splits the two). Sibling encode_rev / encode_rev16 / encode_rbit / encode_clz / encode_cls rejected (different opcode 000010-W/000011-X / 000001 / 000000 / 000100 / 000101). Weaker available: algebraic.metamorphic (Rd/Rn field independence), algebraic.invariant (ARM fields), negative_error (arity / extra / W-form / SP / mixed / FP / invalid NEON T). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for scalar `rev32`; mapping `[Reg(Xd), Reg(Xn)]` <-> `rev32 Xd, Xn`. Direct-call NEON path mapping `[RegArrangement(Vd.T), RegArrangement(Vn.T)]` <-> `rev32 Vd.T, Vn.T` with T in {8b,16b,4h,8h}.
- **Seeds:** bitfield.rs encode_rev16_pbt / encode_rbit_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_rev32 unit tests. README.md:240 lists rev32.

## Module: encode_rev32
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_rev32 (invalid-name / nonreg / alt-spellings / mixed / FP / neon invalid T). Added encode_rev32_diff_alt_spellings, encode_rev32_neg_nonreg, encode_rev32_neg_invalid_name (passing) and encode_rev32_neg_mixed_width, encode_rev32_neg_fp, encode_rev32_neg_neon_invalid_arr (failing, 3 additional bugs). Closed: tier round spent and documented surface covered.
