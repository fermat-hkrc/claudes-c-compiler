# PBT Campaign: encode_neon_float_two_misc

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:226 NEON float vector lists `fneg`/`fabs`/`fsqrt`/`frintn`/`p`/`m`/`z`/`a`/`x`/`i` (vector forms), `frecpe`/`frsqrte`; README.md:236 NEON convert lists `fcvtzs`/`fcvtzu`/`scvtf`/`ucvtf` (vector forms). encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words). Dispatch: encoder/mod.rs:406-458 vector `fneg`/`fabs`/`fsqrt`/`frint*`/`fcvtzs`/`fcvtzu`/`ucvtf`/`scvtf` => encode_neon_float_two_misc. Purpose comment neon.rs:1416-1418: "Encode NEON float two-reg misc: UCVTF, SCVTF, FCVTZS, FCVTZU, FNEG, FABS, etc. (vector) / Format: 0 Q U 01110 size 10000 opcode 10 Rn Rd / size[1]=size_hi, size[0]=sz (0=single, 1=double)". ARM ARM Advanced SIMD two-register miscellaneous (FP): T in {2S,4S,2D}; register is Vd/Vn (v0-v31); extra operands rejected. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding. The producing statements at neon.rs:1421-1430 are not independent Doc evidence.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_neon_float_cmp_zero_kat` (unchanged project harness) → `3 passed; 0 failed; 0 ignored; 0 measured; 2432 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_neon_float_two_misc_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (same convention as neighbouring encode_neon_float_cmp_zero_pbt / encode_neon_float_three_same_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_neon_float_two_misc (single-symbol campaign on neon.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_neon_float_two_misc; other neon.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_neon_* functions
- **State machine:** not applicable — encode_neon_float_two_misc is a pure function (operands + U/size_hi/opcode -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_neon_float_two_misc):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree float two-misc decoder). Sibling encode_neon_two_misc rejected as differential (same-job gate: integer two-misc uses neon_arr_to_q_size over 8b/16b/4h/8h/2s/4s/1d/2d, different size map). Sibling encode_neon_float_cmp_zero rejected (compare-with-zero opcodes and `#0.0` asm form; different job). Sibling encode_neon_float_three_same rejected (three-register, bit 21=1 / opcode at [15:11]). Sibling encode_neon_scalar_two_misc rejected (scalar, not vector). Weaker available: algebraic.metamorphic (U bit, size_hi bit, 2s-vs-4s Q), algebraic.invariant (ARM fields), negative_error (arity / extra / T / mismatch / non-v). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for vector fneg/fabs/fsqrt/frint*/fcvtzs/fcvtzu/ucvtf/scvtf; mapping `[RegArrangement(Vd, T), RegArrangement(Vn, T)] + (U, size_hi, opcode)` <-> `{mnem} Vd.T, Vn.T` with ARM-correct (U, size_hi, opcode).
- **Seeds:** neon.rs encode_neon_float_cmp_zero_pbt (same encoding class, 2-operand llvm-mc differential, ARM fields, extra/mismatch/non-v negatives). neon.rs encode_neon_float_three_same_pbt (float size_hi/sz/Q). No dedicated encode_neon_float_two_misc unit tests.

## Module: encode_neon_float_two_misc
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_neon_float_two_misc (alt-spellings / SP aliases). Added encode_neon_float_two_misc_diff_alt_spellings (passing) and encode_neon_float_two_misc_neg_sp (failing, filed). Closed: tier round spent and documented surface covered.
