# PBT Campaign: encode_bfm

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:216 Bit fields lists `bfm`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:891 `"bfm" => encode_bfm(operands)`. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding. Callers: encoder/mod.rs:891 only. The producing statements at bitfield.rs:91-101 are not independent Doc evidence. Purpose comment bitfield.rs:90: "Encode BFM Rd, Rn, #immr, #imms (bitfield move)". ARM ARM Bitfield Move BFM: sf 01 100110 N immr imms Rn Rd, N=sf; 32-bit immr/imms in [0,31], 64-bit in [0,63]; register 31 is ZR not SP. llvm-mc/gas reject mixed W/X, SP/WSP, FP/SIMD, extra operands, too few operands, and out-of-range immr/imms.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_bfi_kat_llvm_mc_w0_w1_lsb0_width1` (unchanged project harness) → `1 passed; 0 failed; 0 ignored; 0 measured; 2316 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_bfm_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_bfi_pbt / encode_ubfiz_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_bfm (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_bfm; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_sbfiz / encode_ubfiz / encode_bfi / encode_bfxil / encode_extr / encode_clz / encode_cls / encode_rbit / encode_rev / encode_rev16 / encode_rev32
- **State machine:** not applicable — encode_bfm is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_bfm):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree BFM decoder). Sibling encode_bfi / encode_bfxil rejected as differential (same-job gate fails: alias lsb/width form vs raw immr/imms); used only as algebraic alias after the ARM mapping if needed. Sibling encode_ubfm / encode_sbfm rejected (UBFM opc=10 / SBFM opc=00, different instruction). Weaker available: algebraic.metamorphic (Rd/Rn field independence), algebraic.invariant (ARM fields), negative_error (arity / extra / SP / immr-imms range). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for `bfm`; mapping `[Reg(Rd), Reg(Rn), Imm(immr), Imm(imms)]` <-> `bfm Rd, Rn, #immr, #imms`.
- **Seeds:** bitfield.rs encode_bfi_pbt / encode_bfxil_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_bfm unit tests. README.md:216 lists bfm.

## Module: encode_bfm
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_bfm (invalid-name / nonreg / alt-spellings / mixed / FP / extra / SP / immr-imms). Added encode_bfm_diff_alt_spellings, encode_bfm_neg_nonreg, encode_bfm_neg_invalid_name (passing) and encode_bfm_neg_fp (failing, filed). Closed: tier round spent and documented surface covered.
