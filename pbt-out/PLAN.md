# PBT Campaign: encode_neon_qshrn

## Scan findings
- **Spec:** (none found as a standalone requirement/dev-task document). In-tree contract for this symbol:
  - Encoder module docstring at `src/backend/arm/assembler/encoder/mod.rs:1-7`: Encodes AArch64 instructions into 32-bit machine code words; covers the subset emitted by codegen; always 4 bytes little-endian.
  - Assembler README `src/backend/arm/assembler/README.md:5-14`: accepts the same textual assembly that GCC's gas would consume; GNU-style assembly text as emitted by AArch64 codegen.
  - README NEON-narrow table `src/backend/arm/assembler/README.md:229` lists `sqshrn`/`uqshrn`/`sqrshrn`/`uqrshrn` (+ `2` variants).
  - Dispatch `encoder/mod.rs:637-648`: `sqshrn` (RegArrangement) / `sqshrn2` / `uqshrn` / `uqshrn2` / `sqrshrn` / `sqrshrn2` / `uqrshrn` / `uqrshrn2` => `encode_neon_qshrn(operands, u_bit, is_rounding, is_high)`. Scalar `sqshrn` (non-arrangement dest) is a different symbol (`encode_neon_scalar_qshrn`) and is out of scope.
  - Body at `neon.rs:1496-1510`: arity `len < 3` => Err; `get_neon_reg(0)` for Rd (arrangement discarded); `get_neon_reg(1)` for Rn and source Ta; `get_imm(2)` for shift; Ta in {8h,4s,2d} else Err; `shift == 0 || shift > element_bits` => Err with element_bits = source size 16/32/64; `immhb = element_bits - shift`; Q from is_high; opcode 0b100101 (non-rounding) / 0b100111 (rounding); word = Q<<30 | U<<29 | 0b011110<<23 | immh<<19 | immb<<16 | opcode<<10 | Rn<<5 | Rd. No dest-Tb check. No arity upper bound.
  - Sibling `encode_neon_shrn` (`neon.rs:1436-1450`) is the non-saturating member of the same shift-right-narrow class and checks `shift == 0 || shift > half_bits` with `half_bits = element_bits / 2` (destination element size). Same Ta map, same immhb formula.
  - ARM ARM Advanced SIMD shift by immediate SQSHRN/UQSHRN/SQRSHRN/UQRSHRN: `0 Q U 011110 immh immb opcode Rn Rd` with opcode 100101 (non-rounding) / 100111 (rounding). U=0 signed, U=1 unsigned. Q=0 writes the lower half (Tb in {8B,4H,2S}); Q=1 writes the upper half (`2` suffix, Tb in {16B,8H,4S}). `immh != 0000`; `immh<3>==1` UNDEFINED. `esize = 8 << HighestSetBit(immh)` is the destination element size; `shift = (2 * esize) - UInt(immh:immb)` in range 1 to dest element size. Ta/Tb pairs: 8H→8B/16B (shift 1..8), 4S→4H/8H (shift 1..16), 2D→2S/4S (shift 1..32). Exactly three operands: Vd.Tb, Vn.Ta, #shift.
  - llvm-mc `-triple=aarch64` confirms the pairs, shift ranges, and rejects shift 0 / dest_esize+1 / source_esize, mismatched Tb, extra operand, GPR dest, unsupported Ta.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod <name>` inside source files, discovered by `cargo test --lib`. Filename convention: `#[test] fn` inside the source file's test module. `proptest` is already a `[dev-dependencies]` entry (`Cargo.toml`). Historical `pbt-out/`/`pbt-native/` excluded from layout discovery. No crate-root `tests/` directory. `neon.rs` already has prior-campaign modules (`encode_neon_three_diff_narrow_pbt`, `encode_neon_across_long_pbt`, `encode_neon_float_cmp_zero_pbt`, `encode_neon_sli_pbt`, `encode_neon_float_three_same_pbt`) — not rewritten.
- **Buildability probe:** `cargo test --lib test_ascii_passthrough -- --test-threads=${PBT_TEST_JOBS}` in `/home/toan/github/claudes-c-compiler` → `test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1250 filtered out`. Rung 1 available; project cargo harness builds and runs.
- **Harness placement:** extend existing `cargo test --lib` target (rung 1) by appending a new `#[cfg(test)] mod encode_neon_qshrn_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (inline layout; sibling of `encode_neon_sli_pbt` / `encode_neon_three_diff_narrow_pbt`). Reuse the existing `proptest` dev-dependency. Not pbt-native: project cargo harness builds and runs. Historical `pbt-out/`/`pbt-native/` were excluded from layout discovery. Rung 1 was available (probe passed).
- **Candidate modules:** encode_neon_qshrn (neon.rs)
- **Skipped modules:** (none) — campaign scoped to this single symbol; other functions in neon.rs are indexed but not tested.
- **Oracle (encode_neon_qshrn):** Differential (llvm-mc -triple=aarch64 -show-encoding) for valid Vd.Tb + Vn.Ta + #shift in 1..=dest_esize. State machine rejected: pure function, no lifecycle. Round-trip with an in-tree QSHRN decoder rejected: none exists. encode_neon_shrn / encode_neon_sqshrun / encode_neon_scalar_qshrn fail the same-job sibling gate (different opcode / signed-to-unsigned / scalar vs vector). SUT-boundary: internal-helper of the GNU-style assembler; public contract is gas-compatible AArch64 text. Mapping: `operands` <-> `{sq,uq,sqr,uqr}shrn{,2} Vd.Tb, Vn.Ta, #shift`.
- **Seeds:** encode_neon_three_diff_narrow_pbt / encode_neon_sli_pbt (same file): differential vs llvm-mc, ARM field unpack, extra-operand / dest-Tb / unsupported-Ta / arity negatives. Sibling encode_neon_shrn half_bits range. No existing unit test of encode_neon_qshrn itself. Seed: encode_neon_three_diff_narrow_pbt::encode_neon_three_diff_narrow_diff_llvm_mc / encode_neon_sli_pbt::encode_neon_sli_diff_llvm_mc.
- **State machine:** not applicable — encode_neon_qshrn is a pure function with no mutating operations or lifecycle.

## Module: encode_neon_qshrn
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results (1 coverage-sweep round: coverage_gaps had no profraw; manual arm audit of arity / Ta / get_imm as u32 / get_neon_reg Reg dest+source — added encode_neon_qshrn_neg_shift_i64_trunc and encode_neon_qshrn_neg_reg_source)
