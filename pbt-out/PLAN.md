# PBT Campaign: encode_fp_arith

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:223 Floating point lists `fadd`, `fsub`, `fmul`, `fdiv`, `fmax`, `fmin`, `fmaxnm`, `fminnm`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:377-404 scalar dispatch to `encode_fp_arith` with opcodes 0b0010/0011/0000/0001/0100/0101/0110/0111 (vector `RegArrangement` goes to `encode_neon_float_three_same`). Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `fadd s0, s1, s2` = 0x1e222820). Callers: encoder/mod.rs:377-404 only. The producing statements at fp_scalar.rs:68-79 are not independent Doc evidence. ARM ARM Floating-point data-processing (2 source): M=0 S=0 11110 ftype 1 Rm opcode 10 Rn Rd (ftype 00=S, 01=D, 11=H). opcode 0000=FMUL, 0001=FDIV, 0010=FADD, 0011=FSUB, 0100=FMAX, 0101=FMIN, 0110=FMAXNM, 0111=FMINNM. Register 31 is a valid S/D/H index (no ZR/SP in this class). llvm-mc/gas reject extra operands, mixed S/D, GPR, Q/B/V scalar, SP/WSP, and too few operands. Half-precision H,H,H is accepted by llvm-mc with -mattr=+fullfp16 (`fadd h0, h1, h2` = 0x1ee22820, ftype=11).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_fcvt_rounding_kat_llvm_mc_fcvtzs_w0_s1` (unchanged project harness) → `1 passed; 2154 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_fp_arith_pbt` at the bottom of `src/backend/arm/assembler/encoder/fp_scalar.rs` (same convention as neighbouring encode_fcvt_rounding_pbt / encode_fp_1src_pbt / encode_fmov_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_fp_arith (single-symbol campaign on fp_scalar.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_fp_arith; other fp_scalar.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_fcvt_rounding / encode_fp_1src / encode_int_to_float / encode_fcmp / encode_fcvt_precision / encode_fmov
- **State machine:** not applicable — encode_fp_arith is a pure function (operands + opcode -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_fp_arith):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree FP 2-source decoder). Sibling encode_neon_float_three_same rejected (vector/RegArrangement, different ARM class). Sibling encode_fmadd_fmsub / encode_fp_1src / encode_fneg rejected as differential (same-job gate: 4-operand fused / 1-source / different mnemonic). RISC-V encode_fp_arith rejected (different ISA). Weaker available: algebraic.metamorphic (Rd/Rn/Rm field independence; S vs D ftype; opcode), algebraic.invariant (ARM fields), negative_error (arity / extra / mixed S-D / GPR / SP / QVB / nonreg / invalid-name). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for the eight scalar mnemonics; mapping `[Reg(Sd|Dd), Reg(Sn|Dn), Reg(Sm|Dm)]+opcode` <-> `fadd|fsub|fmul|fdiv|fmax|fmin|fmaxnm|fminnm Sd|Dd, Sn|Dn, Sm|Dm`.
- **Seeds:** fp_scalar.rs encode_fp_1src_pbt / encode_fmov_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_fp_arith unit tests. README.md:223 lists the eight scalar mnemonics.

## Module: encode_fp_arith
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_fp_arith (invalid-name / parse_reg_num failure). Added encode_fp_arith_neg_invalid_name (passing). Closed: tier round spent and documented surface covered.
