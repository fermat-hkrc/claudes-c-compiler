# PBT Campaign: encode_rbit

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:240 Bit manipulation lists `rbit`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:902-909 `"rbit"` + first operand RegArrangement => encode_neon_rbit else encode_rbit. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `rbit w0, w1` = 0x5ac00020). Callers: encoder/mod.rs:902-909 only. The producing statements at bitfield.rs:167-184 are not independent Doc evidence. ARM ARM Data-processing (1 source) RBIT: sf 1 0 11010110 00000 000000 Rn Rd; register 31 is ZR not SP. llvm-mc/gas reject extra operands, mixed W/X, SP/WSP, FP/SIMD, and too few operands. The function also encodes NEON RBIT Vd.T, Vn.T when the first operand is RegArrangement (T in {8b,16b}); dispatch routes that form to encode_neon_rbit, so the NEON arm is only reached by a direct call.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_clz_kat_llvm_mc_w0_w1` (unchanged project harness) → `1 passed; 2176 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_rbit_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_clz_pbt / encode_cls_pbt / encode_extr_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_rbit (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_rbit; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_bfi / encode_bfxil / encode_extr / encode_clz / encode_cls
- **State machine:** not applicable — encode_rbit is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_rbit):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree RBIT decoder). Sibling encode_neon_rbit rejected as differential for the scalar path (same-job gate: vector/RegArrangement, different ARM class; dispatch already splits the two). Sibling encode_clz / encode_cls / encode_rev rejected (different opcode 000100 / 000101 / 000010). Weaker available: algebraic.metamorphic (Rd/Rn field independence; W vs X sf), algebraic.invariant (ARM fields), negative_error (arity / extra / mixed W-X / SP / FP / nonreg / invalid-name). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for scalar `rbit`; mapping `[Reg(Wd|Xd), Reg(Wn|Xn)]` <-> `rbit Wd|Xd, Wn|Xn`.
- **Seeds:** bitfield.rs encode_clz_pbt / encode_cls_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_rbit unit tests. README.md:240 lists rbit.

## Module: encode_rbit
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_rbit (invalid-name / nonreg / alt-spellings / neon path). Added encode_rbit_diff_alt_spellings, encode_rbit_neg_nonreg, encode_rbit_neg_invalid_name, encode_rbit_diff_valid_neon (passing). Closed: tier round spent and documented surface covered.
