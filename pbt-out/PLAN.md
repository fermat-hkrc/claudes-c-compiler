# PBT Campaign: encode_rev16

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:240 Bit manipulation lists `rev16`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:576-578 `"rev16"` + first operand RegArrangement => encode_neon_two_misc else encode_rev16. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding (gas aarch64-linux-gnu-as agrees on `rev16 w0, w1` = 0x5ac00420). Callers: encoder/mod.rs:576-578 only. The producing statements at bitfield.rs:196-202 are not independent Doc evidence. ARM ARM Data-processing (1 source) REV16: sf 1 0 11010110 00000 000001 Rn Rd; register 31 is ZR not SP. llvm-mc/gas reject extra operands, mixed W/X, SP/WSP, FP/SIMD, and too few operands. Vector REV16 Vd.T, Vn.T is a different ARM class (Advanced SIMD two-register miscellaneous) and is dispatched to encode_neon_two_misc, not encode_rev16.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_rbit_kat_llvm_mc_w0_w1` (unchanged project harness) → `1 passed; 2198 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_rev16_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_rbit_pbt / encode_clz_pbt / encode_cls_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_rev16 (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_rev16; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_bfi / encode_bfxil / encode_extr / encode_clz / encode_cls / encode_rbit
- **State machine:** not applicable — encode_rev16 is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_rev16):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree REV16 decoder). Sibling encode_neon_two_misc rejected as differential for the scalar path (same-job gate: vector/RegArrangement, different ARM class; dispatch already splits the two). Sibling encode_rev / encode_rev32 / encode_rbit / encode_clz / encode_cls rejected (different opcode 000010/000011 / 000010 / 000000 / 000100 / 000101). Weaker available: algebraic.metamorphic (Rd/Rn field independence; W vs X sf), algebraic.invariant (ARM fields), negative_error (arity / extra / mixed W-X / SP / FP / nonreg / invalid-name). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for scalar `rev16`; mapping `[Reg(Wd|Xd), Reg(Wn|Xn)]` <-> `rev16 Wd|Xd, Wn|Xn`.
- **Seeds:** bitfield.rs encode_rbit_pbt / encode_clz_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_rev16 unit tests. README.md:240 lists rev16.

## Module: encode_rev16
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_rev16 (invalid-name / nonreg / alt-spellings). Added encode_rev16_diff_alt_spellings, encode_rev16_neg_nonreg, encode_rev16_neg_invalid_name (passing). Closed: tier round spent and documented surface covered.
