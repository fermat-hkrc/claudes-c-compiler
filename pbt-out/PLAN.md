# PBT Campaign: encode_neon_ldnr

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:12 ("accepts the same textual assembly that GCC's gas would consume"), README.md:235 NEON load/store table lists `ld1r`/`ld2r`/`ld3r`/`ld4r` (with post-index). encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words). Dispatch: encoder/mod.rs:655-657 `"ld2r" => encode_neon_ldnr(operands, 2)`; `"ld3r" => encode_neon_ldnr(operands, 3)`; `"ld4r" => encode_neon_ldnr(operands, 4)`. Purpose comment neon.rs:1527 "NEON LD2R/LD3R/LD4R". Independent assembler references: llvm-mc -triple=aarch64 -show-encoding and aarch64-linux-gnu-as (agree on encodings). ARM AdvSIMD load/store single structure (replicate): 0 Q 001101 L R=1 S Rm opcode size Rn Rt with opcode 110 (LD1R/LD2R) / 111 (LD3R/LD4R) and S at bit 21 (0 for LD1R/LD3R, 1 for LD2R/LD4R); no-offset Rm=00000 L=0; imm post-index Rm=11111 L=1; register post-index Rm=Xm L=1. Immediate post-index #imm must equal num_structs*esize. Base is Xn|SP (not XZR/W/x31). Register list is consecutive wrapping. The producing statements at neon.rs:1528-1572 are not independent Doc evidence.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_ldrsw_kat_llvm_mc_x0_x1` (unchanged project harness) → `2 passed; 0 failed; 0 ignored; 0 measured; 2577 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_neon_ldnr_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (same convention as neighbouring encode_neon_dup_pbt / encode_neon_tbl_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_neon_ldnr (single-symbol campaign on neon.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_neon_ldnr; other neon.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_* functions
- **State machine:** not applicable — encode_neon_ldnr is a pure function (operands, num_structs -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_neon_ldnr):** Differential vs llvm-mc `-triple=aarch64 -show-encoding` (gas aarch64-linux-gnu-as agrees on KAT vectors). Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree LD2R/LD3R/LD4R decoder). Sibling encode_neon_ld1r rejected as differential (same-job gate: LD1R is a different mnemonic/dispatch; encode_neon_ldnr is called only for n=2/3/4). Sibling encode_neon_ld_st_single / encode_neon_ld_st_multi rejected (single-element / multiple-structure, not replicate). Weaker available: algebraic.metamorphic (Rt/Rn/Q/L), algebraic.invariant (ARM fields), negative_error (arity / extra / count / arrangement / W-base / XZR-base / offset / non-consecutive). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for `ld2r`/`ld3r`/`ld4r`; mapping `[RegList({Vt.T, Vt+1.T, ...}), Mem{Xn|SP} | MemPostIndex{Xn|SP, #n*esize}]` <-> `ldNr {Vt.T, ...}, [Xn|SP{], #imm}]`.
- **Seeds:** neon.rs encode_neon_tbl_pbt (RegList + llvm-mc differential), encode_neon_ld1r body (replicate load encoding), load_store.rs encode_ldrs_pbt (Mem/MemPostIndex differential, extra/invalid-base negatives). No dedicated encode_neon_ldnr unit tests.

## Module: encode_neon_ldnr
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit added mixed-arrangement regression and LD3R-only differential (1000 cases). Closed: tier round spent.
