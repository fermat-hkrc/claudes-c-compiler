# PBT Campaign: encode_ubfiz

## Scan findings
- **Spec:** (none found) as a dedicated requirement/PR doc. Contract taken from assembler README.md:11 ("accepts the same textual assembly that GCC's gas would consume"), README.md:216 Bit fields lists `ubfiz`, encoder/mod.rs:1-7 (Encodes AArch64 instructions into 32-bit machine code words), encoder/mod.rs:889 `"ubfiz" => encode_ubfiz(operands)`. Independent assembler reference: llvm-mc -triple=aarch64 -show-encoding. Callers: encoder/mod.rs:889 only. The producing statements at bitfield.rs:76-88 are not independent Doc evidence. Purpose comment bitfield.rs:75: "Encode UBFIZ Rd, Rn, #lsb, #width — alias for UBFM Rd, Rn, #(-lsb MOD regsize), #(width-1)". ARM ARM Bitfield Move UBFIZ alias of UBFM: sf 10 100110 N immr imms Rn Rd, N=sf, immr=(-lsb MOD datasize), imms=width-1; register 31 is ZR not SP. llvm-mc/gas reject mixed W/X, SP/WSP, FP/SIMD, extra operands, too few operands, and out-of-range lsb/width (0<=lsb<R, 1<=width<=R-lsb).
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod encode_*_pbt` blocks in `src/backend/arm/assembler/encoder/*.rs` (no `tests/` directory, no `[[test]]` in Cargo.toml). Runner: `cargo test --lib`. PBT framework: `proptest = "1.11.0"` in `[dev-dependencies]`. Historical `pbt-native/` / `pbt-out/` excluded from layout discovery.
- **Buildability probe:** `cargo test --lib encode_bfi_kat_llvm_mc_w0_w1_lsb0_width1` (unchanged project harness) → `1 passed; 0 failed; 2292 filtered out`. Rung 1 available.
- **Harness placement:** extend existing `cargo test --lib` target; add `#[cfg(test)] mod encode_ubfiz_pbt` at the bottom of `src/backend/arm/assembler/encoder/bitfield.rs` (same convention as neighbouring encode_sbfiz_pbt / encode_bfi_pbt). Not pbt-native: probe succeeded. Do not rewrite already-covered encode_*_pbt modules in this file.
- **Candidate modules:** encode_ubfiz (single-symbol campaign on bitfield.rs)
- **Skipped modules:** (none) — campaign is restricted to encode_ubfiz; other bitfield.rs symbols stay out of scope in FUNCTION_INDEX except previously covered encode_sbfiz / encode_bfi / encode_bfxil / encode_extr / encode_clz / encode_cls / encode_rbit / encode_rev / encode_rev16 / encode_rev32
- **State machine:** not applicable — encode_ubfiz is a pure function (operands -> Word/Err) with no lifecycle or mutating operations
- **Oracle (encode_ubfiz):** Differential vs llvm-mc `-triple=aarch64 -show-encoding`. Stronger rejected: State machine (no lifecycle). Algebraic round-trip rejected (no in-tree UBFIZ decoder). Sibling encode_ubfm rejected as differential (same-job gate fails: raw immr/imms form vs alias lsb/width); used only as algebraic alias after the ARM mapping. Sibling encode_sbfiz rejected (SBFM opc=00, different instruction). Sibling encode_ubfx rejected (different alias: immr=lsb, imms=lsb+width-1). Sibling encode_bfi rejected (BFM opc=01). Weaker available: algebraic.metamorphic (UBFIZ alias of UBFM; Rd/Rn field independence), algebraic.invariant (ARM fields), negative_error (arity / extra / SP / lsb-width). SUT-boundary: internal-helper of the GNU-style assembler, caller-reachable from encode_instruction for `ubfiz`; mapping `[Reg(Rd), Reg(Rn), Imm(lsb), Imm(width)]` <-> `ubfiz Rd, Rn, #lsb, #width`.
- **Seeds:** bitfield.rs encode_sbfiz_pbt / encode_bfi_pbt (llvm-mc differential, ARM fields, extra/wrong-type negative contracts). No dedicated encode_ubfiz unit tests. README.md:216 lists ubfiz.

## Module: encode_ubfiz
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results
- Sweep round 1/1: coverage_gaps had no LLVM profraw; manual arm audit of encode_ubfiz (invalid-name / nonreg / alt-spellings / mixed / FP). Added encode_ubfiz_diff_alt_spellings, encode_ubfiz_neg_nonreg, encode_ubfiz_neg_invalid_name (passing) and encode_ubfiz_neg_mixed_width / encode_ubfiz_neg_fp (failing, filed). Closed: tier round spent and documented surface covered.
