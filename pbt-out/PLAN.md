# PBT Campaign: encode_neon_sli

## Scan findings
- **Spec:** (none found as a standalone requirement/dev-task document). In-tree contract for this symbol:
  - Encoder module docstring at `src/backend/arm/assembler/encoder/mod.rs:1-7`: Encodes AArch64 instructions into 32-bit machine code words; covers the subset emitted by codegen; always 4 bytes little-endian.
  - Assembler README `src/backend/arm/assembler/README.md:5-14`: accepts the same textual assembly that GCC's gas would consume; GNU-style assembly text as emitted by AArch64 codegen.
  - Assembler README `src/backend/arm/assembler/README.md:228`: NEON shifts category lists `sli` (with `shl`, `sri`, `sshr`, `ushr`, …).
  - Dispatch `encoder/mod.rs:661`: `"sli" => encode_neon_sli(operands)`.
  - Body at `neon.rs:1257-1282`: Encode NEON SLI (shift left and insert). Format `SLI Vd.T, Vn.T, #shift` / `0 Q 1 0 11110 immh:immb 010101 Rn Rd (U=1)`. Arity `operands.len() < 3` → Err. Dest Q from `neon_arr_to_q_size`; src arrangement discarded. `immh:immb` from dest T: 8b/16b → `(8+shift)&0xF`, 4h/8h → `(16+shift)&0x1F`, 2s/4s → `(32+shift)&0x3F`, 2d → `(64+shift)&0x7F`.
  - ARM ARM Advanced SIMD shift by immediate (`SLI <Vd>.<T>, <Vn>.<T>, #<shift>`). Encoding: `0 Q U 011110 immh:immb 010101 Rn Rd` with U=1. `immh:immb = esize + shift`. Valid T and shift: 8B (Q=0, shift 0..7), 16B (Q=1, shift 0..7), 4H (Q=0, shift 0..15), 8H (Q=1, shift 0..15), 2S (Q=0, shift 0..31), 4S (Q=1, shift 0..31), 2D (Q=1, shift 0..63). 1D (Q=0 and immh=1xxx) is reserved for the vector encoding (scalar `SLI Dd, Dn, #shift` is a different class, bits[31:30]=01). llvm-mc `-triple=aarch64` confirms: `sli v0.8b, v1.8b, #0` = 0x2f085420; `sli v0.8b, v1.8b, #7` = 0x2f0f5420; `sli v0.16b, v1.16b, #3` = 0x6f0b5420; `sli v0.4h, v1.4h, #15` = 0x2f1f5420; `sli v0.8h, v1.8h, #0` = 0x6f105420; `sli v0.2s, v1.2s, #31` = 0x2f3f5420; `sli v0.4s, v1.4s, #16` = 0x6f305420; `sli v0.2d, v1.2d, #0` = 0x6f405420; `sli v0.2d, v1.2d, #63` = 0x6f7f5420. Rejects: extra operand, T mismatch, T=1d/8s, v32, GPR dest, shift outside [0, esize-1], too few operands. Scalar `sli d0, d1, #0` = 0x7f405420 is a different encoding — not this vector helper.
  - `get_neon_reg` (`neon.rs:7-21`) accepts `RegArrangement` or `Reg` (empty arrangement); `parse_reg_num` (`encoder/mod.rs:131-148`) maps sp/wsp/xzr/wzr→31, lr→30, and x/w/d/s/q/v/h/b prefixes with num<=31. `get_imm` (`encoder/mod.rs:968-973`) requires `Operand::Imm`.
  - No codegen emission of the `sli` mnemonic (assembler surface only). Sibling `encode_neon_shl` (`neon.rs:1231`) is the U=0 encoding of the same format (different job: SHL vs SLI). Sibling `encode_neon_shift_left_imm` (`neon.rs:1744`) is the parameterized SQSHL/UQSHL/SHL helper; dispatcher does not route `sli` through it.
- **Test layout:** Project-owned tests are inline `#[cfg(test)] mod <name>` inside source files, discovered by `cargo test --lib`. Filename convention: `#[test] fn` inside the source file's test module. `proptest` is already a `[dev-dependencies]` entry (`Cargo.toml`). Historical `pbt-out/`/`pbt-native/` excluded from layout discovery. No crate-root `tests/` directory. `neon.rs` already has prior-campaign modules `encode_neon_three_diff_narrow_pbt`, `encode_neon_across_long_pbt`, and `encode_neon_float_cmp_zero_pbt` — not rewritten.
- **Buildability probe:** `cargo test --lib encode_neon_float_cmp_zero_kat_llvm_mc_fcmeq_v0_4s_v1_4s -- --test-threads=${PBT_TEST_JOBS}` in `/home/toan/github/claudes-c-compiler` → `test result: ok. 1 passed; 0 failed; 1041 filtered out`. Rung 1 available; project cargo harness builds and runs.
- **Harness placement:** extend existing `cargo test --lib` target (rung 1) by appending a new `#[cfg(test)] mod encode_neon_sli_pbt` at the bottom of `src/backend/arm/assembler/encoder/neon.rs` (inline layout; sibling of `encode_neon_float_cmp_zero_pbt`). Reuse the existing `proptest` dev-dependency. Not pbt-native: project cargo harness builds and runs. Historical `pbt-out/`/`pbt-native/` were excluded from layout discovery. Rung 1 was available (probe passed).
- **Candidate modules:** encode_neon_sli (neon.rs)
- **Skipped modules:** (none) — campaign scoped to this single symbol; other functions in neon.rs are indexed but not tested.
- **Oracle (encode_neon_sli):** Differential (llvm-mc -triple=aarch64 -show-encoding) for valid vector SLI with T in {8b,16b,4h,8h,2s,4s,2d}, Vd/Vn in v0–v31, shift in [0, esize(T)-1]. State machine rejected: pure function, no lifecycle. Round-trip with an in-tree SLI decoder rejected: none exists. encode_neon_shl / encode_neon_shift_left_imm fail the same-job sibling gate (SHL vs SLI; SQSHL/UQSHL helper vs dedicated SLI). SUT-boundary: internal-helper of the GNU-style assembler; public contract is gas-compatible AArch64 text. Mapping: `[RegArrangement(Vd, T), RegArrangement(Vn, T), Imm(shift)]` <-> `sli Vd.T, Vn.T, #shift`.
- **Seeds:** encode_neon_float_cmp_zero_pbt (same file): differential vs llvm-mc, ARM field unpack, extra-operand / invalid-arrangement / dest-type negatives. README.md:228 lists sli. No existing unit test of encode_neon_sli. Seed: encode_neon_float_cmp_zero_pbt::encode_neon_float_cmp_zero_diff_llvm_mc.
- **State machine:** not applicable — encode_neon_sli is a pure function with no mutating operations or lifecycle.

## Module: encode_neon_sli
- [x] Scan: identify targets
- [x] Plan: formalize properties
- [x] Test: write and run
- [x] Review: triage results (1 coverage-sweep round: coverage_gaps had no profraw; manual arm audit of get_neon_reg Operand::Reg dest / src — dest Reg Err as specified; src Reg is bug 3)
